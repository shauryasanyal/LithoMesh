# LithoMesh
LithoMesh is an experimental offline-first reconciliation engine that minimizes synchronization overhead after long network partitions using compact state summaries and opportunistic recovery.
